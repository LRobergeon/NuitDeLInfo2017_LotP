version: '2'
services:
    sarouleman-mongodb:
        image: mongo:3.4.8
        ports:
            - "27017:27017"
        command: mongos --configdb sarouleman-mongodb-config
    sarouleman-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    sarouleman-mongodb-config:
        image: mongo:3.4.8
        container_name: sarouleman-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
